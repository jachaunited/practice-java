/*import java.io.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;*/
import java.util.*;

public class stringToChar {

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        String[] s = new String[n];
        String tempOdd = "", tempEven = "";        
        /*(String tempOdd = "", tempEven = "";
        char[] S;
        char[] sCharArr;
        int[] nLength = new int[n];
        String[] odd = new String[n];
        String[] even = new String[n];
        List<String> even = new ArrayList<>();
        List<String> odd = new ArrayList<>();
        for(int i = 0; i < n; i++) {
        	sCharArr = in.next().toCharArray();
        	nLength[i] = sCharArr.length;
        	for(int j = 0; j < sCharArr.length; j++) {
        		if(j % 2 == 0) {
        			even.add(Character.toString(sCharArr[j]));
        		}
        		else {
        			odd.add(Character.toString(sCharArr[j]));
        		}
        	}
        	odd[i] = tempOdd;
        	even[i] = tempEven;
        	tempOdd = ""; tempEven = "";
        	System.out.println(even + " " + odd);
        	even.clear();
        	odd.clear();
       }
       for(int i = 0; i < n; i++) {
    	   for(int j = 0; j < nLength[i]; j++) {
    		   System.out.print(even.get(j));
    		   System.out.print(odd.get(j));
    	   }
       }*/
   }
}